<nav>
    <div class="nav-left">
        <a href="{{ $locale === 'en' ? '/en/' : '/' }}" class="logo">SEOLink<span>Place</span></a>
        <div class="nav-links">
            @if($landing ?? false)
                <a href="{{ $locale === 'en' ? '#how' : '#how' }}" onclick="closeMobile()">{{ $locale === 'en' ? 'How it works' : 'Як це працює' }}</a>
                <a href="#for-who" onclick="closeMobile()">{{ $locale === 'en' ? 'For who' : 'Для кого' }}</a>
                <a href="#tasks" onclick="closeMobile()">{{ $locale === 'en' ? 'Tasks' : 'Завдання' }}</a>
                <a href="#apply" onclick="closeMobile()">{{ $locale === 'en' ? 'Apply' : 'Заявка' }}</a>
            @endif
            <a href="{{ $locale === 'en' ? route('blog.index.en') : route('blog.index') }}" {{ request()->is('blog*') || request()->is('en/blog*') ? 'style=color:#eab308;font-weight:700;' : '' }}>{{ $locale === 'en' ? 'Blog' : 'Блог' }}</a>
            <a href="{{ $locale === 'en' ? route('contact.index.en') : route('contact.index') }}">{{ $locale === 'en' ? 'Contact' : 'Контакти' }}</a>
        </div>
    </div>
    <div class="nav-right">
        <button class="theme-toggle" id="themeToggle" title="{{ $locale === 'en' ? 'Toggle theme' : 'Змінити тему' }}" aria-label="{{ $locale === 'en' ? 'Toggle theme' : 'Змінити тему' }}">
            <svg class="icon-sun" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41"/></svg>
            <svg class="icon-moon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
        </button>
        <a href="{{ route('unified.login') }}" class="nav-login">{{ $locale === 'en' ? 'Sign in' : 'Увійти' }}</a>
        <a href="{{ route('unified.register') }}" class="nav-register">{{ $locale === 'en' ? 'Sign up' : 'Реєстрація' }}</a>
        @if($locale === 'en')
            <a href="/" class="nav-lang">UK</a>
        @else
            <a href="/en/" class="nav-lang">EN</a>
        @endif
    </div>
    <button class="nav-burger" id="navBurger" aria-label="{{ $locale === 'en' ? 'Menu' : 'Меню' }}">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
    </button>
</nav>
<div class="nav-mobile" id="navMobile">
    @if($landing ?? false)
        <a href="#how" onclick="closeMobile()">{{ $locale === 'en' ? 'How it works' : 'Як це працює' }}</a>
        <a href="#for-who" onclick="closeMobile()">{{ $locale === 'en' ? 'For who' : 'Для кого' }}</a>
        <a href="#tasks" onclick="closeMobile()">{{ $locale === 'en' ? 'Tasks' : 'Завдання' }}</a>
        <a href="#apply" onclick="closeMobile()">{{ $locale === 'en' ? 'Apply now' : 'Залишити заявку' }}</a>
    @endif
    <a href="{{ $locale === 'en' ? route('blog.index.en') : route('blog.index') }}" onclick="closeMobile()">{{ $locale === 'en' ? 'Blog' : 'Блог' }}</a>
    <a href="{{ $locale === 'en' ? route('contact.index.en') : route('contact.index') }}" onclick="closeMobile()">{{ $locale === 'en' ? 'Contact' : 'Контакти' }}</a>
    <a href="{{ route('unified.login') }}">{{ $locale === 'en' ? 'Sign in' : 'Увійти' }}</a>
    @if($locale === 'en')
        <a href="/">UK</a>
    @else
        <a href="/en/">EN</a>
    @endif
</div>
